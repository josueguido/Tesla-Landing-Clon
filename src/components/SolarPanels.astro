---
import Section from "./Section.astro";
---

<Section
    title="Solar Panels"
    subtitle="Schedule a Virtual Consultation"
    color="black"
    id="Solar Panels"
>
    <div slot="footer" class="gap-x-6 flex mx-auto">
        <a
            class="bg-white bg-opacity-30 p-4 text-sm rounded font-medium text-black px-20 py-2 transition-colors hover:bg-black hover:text-white inline-flex justify-center items-center"
            href="#"
        >
            Order Now</a
        >
        <a
            class="bg-black bg-opacity-30 p-4 text-sm rounded font-medium text-white px-20 py-2 transition-colors hover:bg-white hover:text-black inline-flex justify-center items-center"
            href="#"
        >
            Learn More</a
        >
    </div>

    <img
        slot="background"
        alt="Solar Panels"
        class="h-full w-full object-cover object-center"
        src="SolarPanels.avif"
        id="bg-image-SolarPanels"
    />
</Section>

<script>
    function isEdge() {
        if (
            navigator.userAgent.indexOf("Edge") > -1 ||
            navigator.userAgent.indexOf("Edg") > -1
        ) {
            return true;
        } else {
            return false;
        }
    }

    var imageElement = document.querySelector(
        "#bg-image-powerwall",
    ) as HTMLImageElement;

    if (isEdge()) {
        imageElement.src = "powerwall.webp";
    } else {
        imageElement.src = "powerwall.avif";
    }
</script>
